---

## title: 仓颉学习笔记  
date: 2025-06-01  
author: Danny  
categories:  
  - 编程语言  
tags:  
  - Cangjie  
  - 学习笔记
# Cangjie编程语言学习笔记
> 前言：
>

> 这篇学习笔记用于记录我个人学习的收获，不太适合将仓颉作为第一编程语言的学习者，如果要入门的话，可以阅读我的另外2个专栏，下面会给出链接：
>

## 1.仓颉语言概述
仓颉编程语言是一款面向全场景智能的新一代编程语言，主打原生智能化、天生全场景、高性能、强安全。主要应用于鸿蒙原生应用及服务应用等场景中，为开发者提供良好的编程体验。

四个主要优势：

+ **智能化：**内嵌AgentDSL的编程框架，自然语言&编程语言有机融合；多Agent协同，简化符号表达，模式自由组合，支持各类智能应用开发
+ **全场景：**轻量化可缩放运行时，模块化分层设计，内存再小也能装得下；全场景领域扩展，元编程和eDSL技术，支持面向领域声明式开发
+ **高性能：**终端场景首款全并发 GC ，应用线程更流畅，响应更快。轻量化线程，并发性能更好，开销更少
+ **强安全：**安全DNA融入语言设计，帮助开发者专注于业务逻辑，免于将太多精力投入到防御性编程中，编码即安全，漏洞无处藏

### 特性1 AgentDSL
**AgentDSL** ：是一种面向智能代理（Agent）的领域特定语言（Domain Specific Language, DSL），专门用于构建具备自主行为、决策能力的智能体。将 AgentDSL 内嵌到主语言中，意味着开发者可以直接在代码中使用自然语言风格来定义 Agent 的行为逻辑，

```plain
agent UserBehavior {
when user_clicks_button -> recommend_next_item()
}
```

这种方式降低了开发智能应用的门槛，使得非专业人员也能理解甚至参与设计

### 特性2 **自然语言 & 编程语言有机融合**：
就是像Python那样可以直接输入中文字符作为变量、函数名

### 特性3 **多 Agent 协同**：
系统支持多个 Agent 并行运行，并能相互通信、协作完成复杂任务。比如：一个客服系统中可以有“对话理解 Agent”、“订单处理 Agent”、“用户画像 Agent”等协同工作。

### 特性4 **轻量化可缩放运行时**：
运行环境小巧灵活，适合从云端服务器到边缘设备、IoT 终端等各种硬件环境。可根据资源限制动态调整运行时组件，保证性能与功能的平衡

### 特性5 **模块化分层设计**：
架构上采用模块化设计，各功能层解耦清晰，便于扩展和维护。  
比如：底层是运行时引擎，中间是语言编译器，上层是各种 DSL 插件。

### 特性6 **元编程和 eDSL 技术**：
+ **元编程（Metaprogramming）**：允许程序在运行时修改自身结构或行为，提高灵活性。
+ **eDSL（Embedded Domain Specific Language）**：即嵌入式领域语言，是指在宿主语言中定义的专用语言。

### 特性7 **终端场景首款全并发 GC**：
+ **GC（Garbage Collection）垃圾回收机制**通常会暂停主线程，影响响应速度。
+ “全并发 GC”意味着垃圾回收过程几乎不影响主线程执行，极大提升了终端应用的流畅度。
+ 特别适用于实时交互类应用，如游戏、AR/VR、语音助手等。

### 特性8 协程（Coroutine）与 绿色线程（Green Thread）
#### 协程（Coroutine）
协程是一种允许在特定点暂停执行并在之后恢复的计算单元。不同于进程或线程通过操作系统调度器来管理其执行，协程通常是在用户空间中实现，并由程序员显式地控制何时挂起或恢复执行。这种机制非常适合于需要长时间运行、可以分步骤完成的任务，如I/O操作、网络请求等，因为它们可以在等待外部资源时让出CPU，从而提高效率。

协程的主要优点包括：

+ **轻量**：创建和销毁协程的成本很低。
+ **可预测性**：由于协程的调度是由应用程序本身而不是操作系统管理的，因此更容易预测行为。
+ **简化异步编程**：使用协程可以使异步代码看起来更像同步代码，提高了代码的可读性和维护性。

许多现代编程语言如Python、Kotlin和JavaScript等都提供了对协程的支持。

#### 绿色线程（Green Thread）
绿色线程则是另一种形式的轻量级线程，它完全由运行时环境（runtime environment）而非操作系统管理。这意味着绿色线程可以在单个操作系统线程上同时运行多个逻辑线程，而不需要操作系统进行上下文切换。这使得绿色线程具有较低的开销，尤其是在上下文切换频繁的情况下性能优越。

然而，随着操作系统线程模型的发展以及硬件并发能力的增强，绿色线程逐渐被原生线程所取代。这是因为绿色线程无法直接利用多核处理器的优势，所有绿色线程都在同一个操作系统线程上运行，限制了真正的并行执行。

尽管如此，在某些特定场景下，例如高密度并发的服务端应用中，绿色线程依然有其应用场景。一些语言和框架如Erlang和早期的Java曾广泛使用绿色线程，但现在大多数现代系统倾向于使用原生线程结合高效的事件驱动模型或协程来实现高并发。

### 特性9 **免于防御性编程**：
+ 防御性编程是指为了防止错误而写很多冗余代码，如判断 null、try-catch 等。
+ 该系统通过语言机制自动处理这些问题，从而简化代码、提高可读性。

## 2.基础语法：
### 2.1 数据类型：
数据类型的声明：

```typescript
let variable: Int64 = 1;
```

#### 2.1.1 整数类型：
| 类型 | 表示范围 | 类型 | 表示范围 |
| :---: | :---: | :---: | :---: |
| Int8 |  | UInt8 |  |
| Int16 |  | UInt16 |  |
| Int32 |  | UInt32 |  |
| Int64 |  | UInt64 |  |
| IntNative | **依赖于平台** | UIntNative | **依赖于平台** |


**可以表示二进制、八进制和十六进制**

| 进制 | 前缀 | 举例 |
| :---: | :---: | :---: |
| 二进制 | 0b或0B | 0b101 |
| 八进制 | 0o或0O | 0o27 |
| 十进制 | 无 | 114 |
| 十六进制 | 0x或0X | 0x12 |


没有特别指定类型一般使用Int64，默认最大，尽量避免溢出

#### 2.1.2 浮点类型：
浮点类型用于存储小数，有3种：

Float16（半精度）、Float32（单精度）、Float64（双精度），

同整数类型一样，后面的数字表示二进制位数。

在实际开发中，通常使用Float64类型，因为**Float64精度最高。**

与整数类型一样，浮点类型也可以表示十六进制数，这里不做过多赘述。

#### 2.1.3 布尔类型:
**布尔类型使用Bool标识，它只有两个值：true或false，用于表示逻辑上的真或假**。在实际开发中，常用来作为标记或者各种条件判断等。

```typescript
/*
 * 多行注释
 * 程序的入口，主函数main，它的返回值是Int64
 */
main(): Int64 {
    var flag: Bool = true
    var isBoy: Bool = false
    return 0
}
```

#### 2.1.4 字符类型
字符类型是表示单个Unicode字符的，用**Rune**标识。字符类型分为：

单个字符、转义字符和通用字符，使用单引号（**'**）包裹。

```typescript
/*
 * 多行注释
 * 程序的入口，主函数main，它的返回值是Int64
 */
main(): Int64 {
    var char1: Rune = 'Q'  //单个字符
    var tab: Rune = '\t'   //转义字符
    var char1 = '\u{4567}' //通用字符
    return 0
}
```

#### 2.1.5 字符串
**用String标识。字符串分为2种形式：单行字符串和多行字符串**。

```typescript
/*
 * 多行注释
 * 程序的入口，主函数main，它的返回值是Int64
 */
main(): Int64 {
    var str1: String = "我是Danny"//单行字符
    var str1: String = 
    """
    我是Danny
    """
    // 如果不想用转义字符来写系统路径，可以使用#"包裹
    var str3: String = #"D:\Danny\DirOfDanny>"#
    return 0
}
```

> 值得一提的是，String是一个结构体，而非类，存储于栈上，访问速度更快
>

**String.empty**与**“”**的区别：

| 方面 | `""` | `String.empty` |
| --- | --- | --- |
| **类型** | 字符串字面量（literal） | `String` 类型的静态常量 |
| **可读性** | 简洁直观 | 语义更清晰（显式表达“空字符串”意图） |
| **性能（理论）** | 每次出现可能生成新引用   （取决于编译器优化，我个人猜测仓颉是有的，因为<font style="background-color:#f3bb2f;">C#</font>也有） | 通常指向同一个全局常量实例（更高效） |
| **一致性** | 通用写法 | 符合标准库 API 风格，尤其在泛型或反射场景更规范 |


一般来说，无脑使用`String.empty`

#### 2.1.6 元组类型
**元组类型可以将两个或者两个以上类型组合在一起，形成一个新的类型。元组类型是不可变类型，一旦初始化后，就不能被改变。**

```typescript
/*
 * 多行注释
 * 程序的入口，主函数main，它的返回值是Int64
 */
main(): Int64 {
    var tup1:(Int64, String, Int64) = (1, "Danny", 23)
    var tup2:(id: Int64, name: String, age: Int64) = (2, "Danny", 24)
    return 0
}
```

#### 2.1.7 数组类型
**仓颉语言提供两种数组类型：引用类型的 **`Array<T>`** 和值类型的 **`VArray<T, $N>`。

+ `Array<T>` 是长度固定、元素类型统一的有序序列，支持动态初始化与修改元素，但不支持增删操作。
+ `VArray<T, $N>` 是编译期确定长度的值类型数组，适用于对性能和内存有更高要求的场景，但元素类型受限，且赋值/传参会拷贝整个数组。
+ 此外还有第三种数组类型，动态数组**`ArrayList<T>`**，动态数组的长度可扩展，支持自动扩容，更加灵活，实际开发过程中均使用动态数组

##### **A.**`Array<T>`** 的基本使用**
`T` 是一个泛型，不同元素类型的 `Array` 互不兼容，不能互相赋值。

```typescript
main(): Int64 {
    var a: Array<Int64> = [0, 1, 2] // 初始化
    var b = a // 把a数组的引用地址赋值b
    b[0] = 99
    // 此时 a 和 b 都为 [99, 1, 2]

    var c: Array<Int64> = Array<Int64>(3, repeat: 0) // 使用 repeat 重复初始化元素
    var d: Array<Int64> = Array<Int64>(3, { i => i + 1 }) // 利用Lambda表达式迭代元素
    
    for (i in a) {
        println("a Element: ${i}")
    }
    
    for (i in c) {
        println("c Element: ${i}")
    }
    
    for (i in d) {
        println("d Element: ${i}")
    }

    let slice = a[1..]
    println("Slice size of a: ${slice.size}") // 获取长度：arr.size 输出 2

    return 0
}
```

##### **B.**`VArray<T, $N>`** 的基本使用**
**`<T, $N>`**不能省略，其中 **`$N>`**编译期数组的固定长度（如 `$5`）

```typescript
main(): Int64 {
    var v1: VArray<Int64, $3> = [10, 20, 30]
    var v2 = v1          // 拷贝整个数组
    v2[0] = 999
    // v1 仍为 [10, 20, 30]，v2 为 [999, 20, 30] 这就是值类型数组不同于引用类型之处

    println("v1[0] = ${v1[0]}")
    println("v2.size = ${v2.size}")

    return 0
}
```

至于ArrayList严格来说是对Array的封装类型，不属于基础类型，会在后面讲包使用时进行介绍

#### 2.1.8 区间类型
区间类型用于表示有固定步长的数值序列，用**Range****<****T****>****标识，Range****<****T>是泛型（后面会详细介绍）。区间类型有2种：****“左闭右开”****和****“左闭右闭”**。

每个区间类型都有三个值：start、end、step。step是前后两个相邻元素的差。

书写形式：

```typescript
start..end:step
```

```typescript
/*
 * 多行注释
 * 程序的入口，主函数main，它的返回值是Int64
 */
main(): Int64 {
    // 左闭右开
    var range1: Range<Int64> = 0..5:1
    // 左闭右闭
    var range2: Range<Int64> = 0..=5:1
    // 步长为-2
    var range3: Range<Int64> = 10..=-10:-2
    return 0
}
```

#### 2.1.9 Unit类型
**Unit类型的值只有一个()，表达式以及后续的一些流程控制的返回值都是Unit类型，通常，在Unit类型里只关注副作用，不关注它的值。**

```plain
func a(){
    return 
}
```

a函数的返回类型就是Unit

#### 2.1.10 Nothing类型
**Nothing类型是一个特殊类型，它不包含任何值。是所有类型的子类型（这当中也包括 **`Unit`** 类型）。**

> 以上是仓颉的10种基础数据类型，除了Nothing和Array是**“引用类型”**，其他8个都是**“值类型”**
>
> **值类型的数据，存储的就是其数据本身**
>
> **引用类型存放的不是数据本身，而是数据本身所在地的地址**
>

### 2.2 操作符
#### 2.2.1 赋值操作符
"="右边赋值给左边

#### 2.2.2算数操作符
和Java与JS一致

可以看我的其他文章：

#### 2.2.3关系操作符
和Java与JS一致，

> 仓颉支持类型推断，**定义变量的时候**不写数据类型也可以
>

#### 2.2.4逻辑操作符
**逻辑操作符用于布尔类型的运算，有3个运算符：逻辑与（&&）、逻辑或（||）、逻辑非（!）**

#### 2.2.5位操作符
**6个操作符：!（按位取反）、<<（左移）、>>（右移）、&（按位与）、|（按位或）、^（按位异或）。**

### 2.3 流程控制
#### 2.3.1 分支结构
```typescript
main(): Int64 {
    let month = 3
    if (month == 3 || month == 4 || month == 5) {
        println("现在是春季")
    } else if (month == 6 || month == 7 || month == 8) {
        println("现在是夏季")
    } else if (month == 9 || month == 10 || month == 11) {
        println("现在是秋季")
    } else {
        println("现在是冬季")
    }
    return 0
}
```

#### 2.3.2 循环结构
##### while循环：
```typescript
main(): Int64 {
    var i = 0
    while (i <= 10) {
        println(i)
        i++ // 循环变量自增
    }
    return 0
}
```

##### do-while循环
do-while循环以do开头，后面紧跟“{}”包裹的循环体，然后是while结束，后面紧跟循环条件，该条件必须是布尔类型的表达式

```typescript
    do {
        
    } while (true)//注意：只能是true，是1都不可以
```

> 注意：只能是true，是1都不可以
>

##### for-in循环
for-in循环以for开头，之后跟以“（）”包裹的循环条件，循环条件写法为“（循环变量 in 区间类型）”，最后是“{}”包裹的循环体。

```typescript
main(): Int64 {
    for (i in 0..=10) {
        println(i)
    }
    return 0
}
```

**注意：循环变量的作用范围只限于当前for-in循环内。**

如果使用for-in循环，循环变量不需要参与循环内部的执行时，可以使用通配符（   **_**   ）在循环变量那里进行占位。

```typescript
main(): Int64 {
    for (_ in 0..3) {
        println("我是仓颉开发程序员")
    }
    return 0
}
```

仓颉也有break和countine用于终止循环

> 和if一样，while、do-while、for-in、break、continue也有自己的类型，**while、do-while、for-in的类型是Unit类型**，**break、continue是Nothing类型**。
>

### 2.4 函数
#### 2.4.1 函数的定义
函数的结构：

> func 函数名(参数1: 参数类型, 参数2: 参数类型, ...): 返回值类型{
>
>     函数体
>
> }
>

```typescript
func Hi() {
    println("Hi!")
}
 
main(): Int64 {
    return 0
}
```

仓颉函数参数分为：**非命名参数和命名参数**

| 特性 | 非命名参数（`p: T`） | 命名参数（`p!: T`） |
| --- | --- | --- |
| **定义语法** | `param: Type` | `param!: Type` |
| **调用方式** | 按位置传参 | 必须按名称传参（如 `func(param: value)`） |
| **默认值支持** | 不支持 | 支持（`p!: T = expr`） |
| **在参数列表中的位置** | 必须在所有命名参数**之前** | 必须在所有非命名参数**之后** |
| **可变性** | 均为不可变（函数内不能重新赋值） | 同左 |


**命名函数的主要作用就是支持默认值以及规范传入参数**

```typescript
func add(a: Int64, b: Int64) {
    println("a + b = ${a + b}")
}

func add(a!: Int64, b!: Int64) {
    println("a + b = ${a + b}")
}

//命名参数可以设置默认值
func add(a!: Int64 = 7, b!: Int64 = 8) {
    println("a + b = ${a + b}")
}

//命名参数和非命名参数可以组合使用，但是命名参数必须在非命名参数之后。
func add(a: Int64, b: Int64, c!: Int64) {
    println("a + b = ${a + b}")
    println("${c}: 在该函数里我是多余的")
}

//注意：在仓颉代码中，函数参数是不可变的。
//以下是错误代码：
func add(a: Int64, b!: Int64) {
    a = a + 1
    b++
    println("a + b = ${a + b}")
}
//修正
func add(a: Int64, b!: Int64) {
    var m = a + 1
    var n = b + 1
    println("a + b = ${a + b}")
    println("m + n = ${m + n}")
}

main(): Int64 {
    return 0
}
```

> **注意：return后跟函数返回值，且函数返回值类型必须与函数声明返回类型保持一致，与变量类型一样，函数声明返回类型也可以不写，由仓颉的编译器自动推断。**
>

#### 2.4.2 函数的使用
> 区别在于函数调用时，命名参数传递时要带上参数名称
>

```typescript
func add(a!: Int64, b!: Int64): Int64 {
    return a + b
}
 
func sum(a!: Int64 = 2, b!: Int64 = 3): Int64 {
    return a + b
}
 
main(): Int64 {
    println(add(a: 1, b: 2))//带上参数名称a:  b:
    println(sum())
    println(sum(a: 1, b: 2))
    return 0
}
```

命名参数还有另外一个作用：使用时，会按照命名对号填入对应的操作，而无视函数定义参数的顺序

```typescript
public func show(a!: Int64, b!: Int64, c!: Int64): Unit {
    println("${a} and ${b} and ${c}");
}

main() {
    show(b: 2, c: 3, a: 1);
    show(a: 1, b: 2, c: 3);
    show(a: 1, c: 3, b: 2);
}
```

```bash
cjpm run
1 and 2 and 3
1 and 2 and 3
1 and 2 and 3
```

函数调用有诸多语法糖，详见进阶部分[函数调用语法糖](#函数调用语法糖)

#### 2.4.3 函数的作用域
全局作用域

在仓颉源文件顶层定义的变量或者函数，称为**全局变量或全局函数**，全局作用域包内可见的

```typescript
var num = 2
 
func fun1() {
    num++
    return num
}
 
func fun2() {
    num--
    return num
}
 
main(): Int64 {
    println(num)
    println(fun1())
    println(fun2())
    return 0
}
```

局部作用域

```typescript
// 函数的局部作用域
func fun1() {
    let x = 5
    println(x)
}
 
println(x) // 错误代码
 
main(): Int64 {
    println(x) // 错误代码
 
    // 模板字符串的局部作用域
    println("${var a = 3; var b = 4}")
    println(a) // 错误代码
 
    // 分支结构的局部作用域
    if (true) {
        let num = 5
        println(num)
    }
    println(num) // 错误代码
 
    // 循环结构的局部作用域
    var i = 0
    while(i < 1) {
        var b = 2
        println(b)
    }
    println(b) // 错误代码
    
    return 0
}
```

同名变量

没有相交（交集）作用域的同名变量

```typescript

 
func fun(num!:Int64) {
    return num
}
 
main(): Int64 {
    var num = 5
    println(num)
    println(fun(num: 1))
    return 0
}
```

```typescript
cjpm run
5
1
```

有相交（交集）作用域的同名变量

```typescript

 
var num = 2
 
main(): Int64 {
    println(num)
    var num = 5
    println(num)
    return 0
}
```

```typescript
cjpm run
2
5
```

**注意：在有相交（交集）作用域的同名变量，优先访问作用域小的变量，作用域小的变量覆盖了作用域大的变量。**

2.4.4函数重载

有时同一种功能的函数，有多种实现方式。在同一作用域中，定义了多个函数，这些函数有以下特点：

1. **函数名相同；**
2. **函数参数类型不同；**
3. **函数参数个数不同；**

**就把这些函数称为：函数重载**

```typescript
func add(x: Int64, y: Int64) {
    return x + y
}
 
func add(x: Float64, y: Float64) {
    return x + y
}
 
func add(x: Int64) {
    return x + 1
}
 
main(): Int64 {
    println(add(1, 1))
    println(add(1.2, 0.8))
    println(add(1))
    return 0
}
```

> **注意：如果是改变函数参数的变量名，则不算函数重载。**
>

```typescript

 
func add(x: Int64, y: Int64) {
    return x + y
}
 
func add(a: Int64, b: Int64) {
    return a + b
}
 
main(): Int64 {
    println(add(1, 1))
    println(add(1, 2))
    return 0
}
```

#### 2.4.5 递归
**在函数体内调用了该函数本身，则称该函数是递归函数**

```typescript

 
func fun(n: Int64): Int64 {
    if (n > 0) {
        println(n)
        return fun(n - 1)
    } else {
        return 0
    }
}
 
main(): Int64 {
    println(fun(3))
    return 0
}
```

#### 2.4.6 函数的其他用法
把函数当作变量值

```typescript

 
func add(x: Int64, y: Int64) {
    x + y
}
 
main() {
    println(add(3, 6))
    // 将函数当作变量值赋值给新的变量
    let sum: (Int64, Int64) -> Int64 = add
    println(sum(3, 6))
}
```

> **注意：sum变量的类型是函数类型，也可以不写，编译器会自动推断类型，如果有函数重载，则必须带类型，否则会报错。**
>

```typescript

 
func add(x: Int64, y: Int64) {
    x + y
}
 
// 函数重载
func add(x: Float64, y: Float64) {
    x + y
}
 
main() {
    println(add(3, 6))
    // 将函数当作变量值赋值给新的变量
    let sum1: (Int64, Int64) -> Int64 = add
    println(sum1(3, 6))
 
    // 错误代码
    let sum2 = add
}
```

把函数当作函数参数

```typescript
func add(x: Int64, y: Int64) {
    x + y
}
 
func printResult(x: Int64, y: Int64, fun: (Int64, Int64) -> Int64) {
    println(fun(x, y))
}
 
main() {
    printResult(3, 4, add)
}
```

把函数当作返回值

```typescript

 
func add(x: Int64, y: Int64) {
    x + y
}
 
func result(): (Int64, Int64) -> Int64  {
    add
}
 
main() {
    println(result()(3, 4))
}
```

#### 2.4.7 嵌套函数
定义在函数内部的函数叫做嵌套函数

作用是隐藏内部函数的实现细节，只有外部函数知道它的存在，从而防止内部函数被修改

```typescript

 
// 公式枚举
enum Formula {
    Girth | Area
}
 
// 计算图形平行四边形的周长和面积
func getGirthOrArea(formula: Formula) {
    func girth(width: Int64, height: Int64) {
        (width + height) * 2
    }
 
    func area(width: Int64, height: Int64) {
        width * height
    }
 
    match(formula) {
        case Formula.Area => area
        case Formula.Girth => girth
    }
}
 
main() {
    println(getGirthOrArea(Formula.Girth)(3, 4))
    println(getGirthOrArea(Formula.Area)(3, 4))
}
```

> 嵌套函数是不能使用public、private这类访问控制符的
>

由嵌套函数引出闭包的概念

#### 2.4.8 闭包（Closure）
概念：简单理解就是嵌套函数能够"记住"并访问其外部作用域中的变量，即使外部函数已经执行完毕

<font style="background-color:#f3bb2f;">注意：以下的过程，叫做变量捕获</font>

+ 在嵌套函数里访问本嵌套函数之外定义的局部变量；
+ 在嵌套函数的参数默认值中访问本嵌套函数之外定义的局部变量；
+ 在class类里面定义的嵌套函数中访问this或实例成员变量；
+ 在struct结构体里面定义的嵌套函数中访问this或实例成员变量

> 为什么要讲变量捕获？
>
> 因为被捕获的变量不会销毁，这就是因为外部变量被内部函数“记住”了
>

```typescript
// 外部函数
func outer() {
    let num = 123123
    // 嵌套函数
    func inner() {
        println(num)
    }
    return inner
}
 
main() {
    let fun = outer()
    fun()
}
```

注意：以下情况不属于变量捕获。

+ 对本函数的局部变量的访问；
+ 对全局变量或静态变量的访问；
+ 在构造函数、实例成员函数中对this的访问

```typescript
class Car {
    let count = 10
 
    func outer() {
        func inner() {
            println(count)
        }
        return inner
    }
}
 
main() {
    let car = Car()
    let out = car.outer()
    out()
}
```

闭包执行的特点

闭包允许一个函数记住并访问它的静态作用域。静态作用域根据代码中的变量和函数的声明位置来确定程序运行查找标识符

闭包会为每一次的函数调用创建独立的上下文环境

```typescript


class Count{
    Count(var counter: Int64) {}
}
 
func outer() {
    let count = Count(0)
    func inner() {
        count.counter++		//inner函数"捕获"了count变量
        println("count.counter: ${count.counter}")
    }
    return inner
}
 
main() {
    let fun = outer() 		// 第一次调用outer，创建一个上下文
    fun()             		// 调用的不是outer，而是inner
    fun()
    fun()
 
    print("\n")
    let fun2 = outer() 		// 第二次调用outer，创建一个全新的上下文
    fun2()
    fun2()
    fun2()
}
```

注意：当捕获的局部变量用var修饰，闭包中的嵌套函数只能被直接调用，不能作为表达式使用，而且对变量的捕获具有传递性。

```typescript
func varCaptureExample() {
    // 1. let变量捕获 - 正常情况
    func normalCapture() {
        let letVar = 100  // let变量 换成var return就会报错
        func inner() {
            println("let变量: ${letVar}")
        }
        return inner  // OK: 可以作为返回值
    }
    
    let normalFunc = normalCapture()  // OK: 可以赋值给变量
    normalFunc()  // OK: 可以调用
    
    // 2. var变量捕获 - 受限情况
    func restrictedCapture() {
        var varVar = 200  // var变量
        func inner() {
            varVar = varVar + 1  // 修改var变量
            println("var变量: ${varVar}")
        }
        
        // ERROR: 不能作为返回值！编译错误
        // return inner  
        
        // OK: 只能直接调用
        inner()
    }
    
    restrictedCapture()  // 直接调用包含函数
    
    // ERROR: 不能将捕获var的函数赋值给变量
    // let restrictedFunc = restrictedCapture().inner  // 编译错误
}
```

```typescript
// 捕获传递性演示
func captureTransitivity() {
    var sharedVar = 0  // 外层var变量
    
    func level1() {
        // level1 捕获了 var sharedVar
        sharedVar = sharedVar + 10
        println("Level1: sharedVar = ${sharedVar}")
        
        func level2() { // level2 继承了对 var sharedVar 的捕获
            sharedVar = sharedVar + 1  
            println("Level2: sharedVar = ${sharedVar}")
            
            func level3() { // level3 也继承了限制
                sharedVar = sharedVar + 1
                println("Level3: sharedVar = ${sharedVar}")
            }
            level3()
        }
        level2()
    }
    level1()
    
    println("最终 sharedVar = ${sharedVar}")
}
```

> 为什么有这样的限制？
>
> 内存安全：
>
> + var变量是可变的，需要确保内存安全
> + 直接调用可以更好地控制变量的生命周期
> + 避免悬空指针和内存泄漏
>
> 性能优化：
>
> + 限制表达式使用可以减少运行时开销
> + 编译器可以进行更好的优化
> + 减少闭包捕获的复杂性
>
> 并发安全：
>
> + 限制var捕获可以避免并发访问问题
> + 简化多线程环境下的内存管理
>

闭包让函数"记住"它被创建时的环境，即使创建环境已经消失，函数仍能访问那些变量！

这就是为什么闭包如此强大 - 它提供了一种优雅的方式来封装状态和创建专门化的函数！

#### 2.4.9 函数重载
函数重载决议规则：

+ 当作用域级别不同时，优先选择级别高的作用域内函数，在嵌套函数中，越是内层的作用域越高

```typescript
func 嵌套作用域优先级演示() {
    func overload(){
        return "overload: Global"
    } 
    func outer() {
        func overload(){
            return "overload: outer"
        }
        func middle() {
            func overload(){
                return "overload: middle"
            }
            func inner() {
                func overload(){
                    return "overload: inner"
                }
                println(overload()) // inner 作用域
            }
            println(overload()) // middle 作用域
            inner() // 打印 “overload: inner”
        }
        println(overload()) // outer 作用域
        middle() // 打印 “overload: middle”
    }
    println(overload()) // Global 作用域
    outer() // 打印 “overload: outer”
}
```

+ 当作用域级别相同时，优先选择参数最匹配函数

##### 参数最匹配规则：
+ 规则1: 精确类型匹配优先
+ 规则2: 参数个数匹配
+ 规则3: 参数类型特异性
+ 规则4: 复合参数匹配

这些基础的函数重载这里不多赘述，可以参考我的其他文章学习

### 2.5 结构体
结构体是一种自定义类型，结构体以“struct”为关键字，必须定义在仓颉文件的顶层（全局作用域）

```typescript
/*
* Book是结构体，它是自定义类型，所有结构体的类型都是值类型
*/
struct Book {
    // 在结构体中定义的变量，称为成员变量
    let writer: String // 作者
    let bookName: String // 书名
    let price: Float64 // 价格
    var count: Int64 // 销售量
 
    /*
    * 在结构体中定义的变量，必须通过构造函数进行初始化才可以使用
    */
    init(writer: String, bookName: String, price: Float64, count: Int64) {
        this.writer = writer
        this.bookName = bookName
        this.price = price
        this.count = count
    }
 
}
 
main(): Int64 {
    var book1 = Book("Danny","Welcome To MyBlog", 0, 0)
    println(book1.bookName)
    println(book1.writer)
    println(book1.price)
    println(book1.count)
    return 0
}
```

#### 2.5.1 构造函数
+ 构造函数是特殊的函数，用于初始化结构体里的变量。
+ 构造函数由分为**普通构造函数和主构造函数**

**普通构造函数可以有多个**

> 用init作为函数名
>

**普通构造函数最大的意义就是函数重载**

```typescript
/*
* Book是结构体，它是自定义类型，所有结构体的类型都是值类型
*/
struct Book {
    // 在结构体中定义的变量，称为成员变量
    let writer: String // 作者
    let bookName: String // 书名
    let price: Float64 // 价格
    var count: Int64 // 销售量
 
    /*
    * 普通构造函数
    */
    init(writer: String, bookName: String, price: Float64, count: Int64) {
        this.writer = writer
        this.bookName = bookName
        this.price = price
        this.count = count
    }
 
    /*
    * 普通构造函数（重载）
    */
    init(bookName: String, price: Float64, count: Int64) {
        this.writer = "Danny"
        this.bookName = bookName
        this.price = price
        this.count = count
    }
}
 
main(): Int64 {
    var book1 = Book("Danny","Welcome To MyBlog", 0, 0)
    println(book1.bookName)
    println(book1.writer)
    println(book1.price)
    println(book1.count)
    println()
    var book2 = Book("Welcome To MyBlog", 0, 0)
    println(book2.bookName)
    println(book2.writer)
    println(book2.price)
    println(book2.count)
    return 0
}
```

**主构造函数只能有一个。**

当结构体中的成员变量非常多的时候，如果再写一个包含所有成员变量的普通构造函数十分麻烦，为此，我们可以用主构造函数来代替。

主构造函数的作用就是

> **主构造函数名与结构体相同**
>

```typescript
/*
* Book是结构体，它是自定义类型，所有结构体的类型都是值类型
*/
struct Book {
    // 主构造函数
    Book(let writer: String, let bookName: String, let price: Float64, var count: Int64) {} // 默认都是public
    // Book(private let writer: String, let bookName: String, let price: Float64, var count: Int64) {} // 同时也是可以自定义可见性的
 
    /*
    * 普通构造函数（重载）
    */
    init(bookName: String, price: Float64, count: Int64) {
        this.writer = "Danny"
        this.bookName = bookName
        this.price = price
        this.count = count
    }
}
 
main(): Int64 {
    var book1 = Book("Danny","Welocome To My Blog", 0, 0)
    println(book1.bookName)
    println(book1.writer)
    println(book1.price)
    println(book1.count)
    println()
    var book2 = Book("Welocome To My Blog", 0, 1)
    println(book2.bookName)
    println(book2.writer)
    println(book2.price)
    println(book2.count)
    return 0
}
```

> **注意：在结构体中，除了可以写构造函数外，普通函数也可以写。**
>

> **注意：定义在结构体内的成员变量，一旦被初始化后，就不能在结构体内重新赋值。**
>
> 要修改需要在函数前加上“mut”关键字
>
> 即便使用mut也无法修改不可变变量
>

```typescript
/*
* Book是结构体，它是自定义类型，所有结构体的类型都是值类型
*/
struct Book {
    // 主构造函数
    Book(let writer: String, let bookName: String, let price: Float64, var count: Int64) {}
 
    // 错误代码
    func renewCount() {
        this.count = this.count + 10
    }
    
    // 正确代码
    mut func renewCount() {
        this.count = this.count + 10
    }
}
 
main(): Int64 {
    var book = Book("Danny","Welocome To My Blog", 0, 0)
    println(book.bookName)
    println(book.writer)
    println(book.price)
    println(book.count)
    println()
    return 0
}
```

> this关键字指向当前结构体对象
>

#### 2.5.2 成员访问
**在结构体中的变量和函数，可以分为实例成员和静态成员（成员：变量、函数）。以上的代码，均是实例成员，只有实例成员在结构体中才会用“this”来访问。而静态成员，则通过结构体名直接来访问。**

> **注意：**
>
> + 静态函数里，不能使用实例成员变量，即：静态函数里，不能使用this关键字
> + 结构体不支持递归使用和组合调用
>

```typescript

 
/*
* Book是结构体，它是自定义类型，所有结构体的类型都是值类型
*/
struct Book {
    // 静态变量在使用前必须初始化
    static let publish: String = "Danny"
 
    // 主构造函数
    Book(let writer: String, let bookName: String, let price: Float64, var count: Int64) {}
    
    
    static func URBeautiful() {
        Book.publish = "I Am Waiting for UR Beauty!"
        this.bookName = "Dont use instance member variables"
    }
    
    //结构体不支持递归使用
    var book: Book
    //结构体不支持组合使用
    var writer: Music
}

struct Music{
    Music(var name: String){}
}
 
main(): Int64 {
    var book = Book("Danny","Welcome To Danny‘s Blog’", 0, 0)
    println(book.bookName)
    // 通过结构体名来访问
    println(Book.publish)
    println()
    Book.URBeautiful()
    return 0
}
```

### 2.6 包管理
在仓颉中，一个仓颉工程对应一个模块

> 模块是最小的发布单元
>

一个模块包含多个包

> 包是最小的编译单元
>

一个包可以包含多个仓颉文件

> 一个包内最多只能有一个main函数
>

main函数是程序的入口，不管怎么分包，也不管分包内是否有main函数

> 在顶层包里，必须有main函数，程序才可以编译和运行，否则会报错
>

#### 2.6.1 访问控制
**在仓颉中，提供了四种访问控制，按照控制访问的范围从小到大依次是：private(私有)、默认不写、protected(被保护)、public(公共)。**

| 修饰符 | 本类 | 本包 | 子类 | 所有 |
| --- | --- | --- | --- | --- |
| private | √ |  |  |  |
| 默认不写 | √ | √ |  |  |
| protected | √ | √ | √ |  |
| public | √ | √ | √ | √ |


#### 2.6.2 包的声明和可见性
仓颉工程下的目录src及其子目录下的源文件，需要进行包的声明。

> package Danny
>

> **注意：包的声明必须在第一行。**
>

src下的a文件夹，里面有a.cj，里面有个Danny结构体

```typescript
//Danny/src/a/a.cj
package Danny.a
 
public struct Danny {//没有public无法在其他文件中访问Danny结构体
    public Danny(let name: String, var age: Int64){}//没有同样无法访问这个构造函数
}
```

导入包的方式：

> **导包的时候，需要把Book结构体的名字也写上。**
>
> 同一个包下有很多类，可以使用“*****”来一次性导出
>
> 使用as关键字，对包重命名，比如：Danny太长了，可以用as重命名成Dy
>

```typescript
package Danny
// 包的导入
import Danny.a.Danny
 
main(): Int64 {
    var danny: Danny = Danny("danny", 18)
    return 0
}
```

> 以下是包的声明的规则
>
> + public顶层函数定义中，不能使用包外不可见的类型作为参数类型和返回值类型；
> + public顶层变量不能被声明为包外不可见类型；
> + public类不能继承包外不可见类；
> + public类型不能实现或继承包外不可见接口；
> + public泛型类型不能使用包外不可见类型作为类型参数；
> + public泛型类型的泛型约束不能使用包外不可见类型
>

#### 2.7 字符串操作
##### 2.7.1 将字符串转为字符数组
```typescript
str.toRuneArray()
```

##### 2.7.2 字符串转为字节数组
```typescript
str.toArray()
```

##### 2.7.3 获取字符串的字节长度（**注意：英文字母占1个字节，中文汉字占3个字节**）
```typescript
str.size
```

##### 2.7.4 获取字符串字面长度
```typescript
str.toRuneArray().size
```

##### 2.7.5 判断字符串中是否包含指定字符串
```typescript
str.contains("l")
```

##### 2.7.6  统计指定字符串在指定字符串中出现的次数
```typescript
str.count("n")
```

##### 2.7.7 判断字符串是否以指定字符串开头或者结尾
```typescript
str.startsWith("D")
str.endWith("y")
```

##### 2.7.8 获取指定字符串的索引
```typescript
str.indexOf("n")
str.lastIndexOf("y")
str.indexOf("n", 1)
```

##### 2.7.9 去除字符串的前缀或后缀
**去除后的字符串是新的字符串。**

```typescript
str.trimLeft("D")
str.trimRight("y")
```

##### 2.7.10  替换字符串
```typescript
str.replace("D", "d")
```

##### 2.7.11 字符串的分割和连接
```typescript
main(): Int64 {
    var str = "Hello|,|I am Danny"
    println(str.split("|"))
    println(String.join(str.split("|"), delimiter: ""))
    return 0
}

output:
[Hello, ,, I am Danny]
Hello,I am Danny
```

##### 2.7.12 大小写转换
```typescript
var str = "Hello,I am Danny"
println(str.toAsciiLower())
println(str.toAsciiUpper())
println(str.toAsciiTitle())

output:
hello,i am danny
HELLO,I AM DANNY
Hello,I Am Danny
```

##### 2.7.13  类型转换
```typescript
import std.convert.Parsable
 
main(): Int64 {
    let num = 123
    let float = 123.456
 
    let newNum = num.toString()
    let newFloat = float.toString()
    println(newNum)
    println(newFloat)
    let str = "true" 
    let flag = Bool.tryParse(str)
    println(flag)
    return 0
}

output:
123
123.456000
Some(true)
```

##### 2.7.14  字符串的拼接和构建
**StringBuilder类对字符串的拼接效率远高于String**

```typescript
main(): Int64 {
    let stringBuilder = StringBuilder()
    stringBuilder.append("H")
    stringBuilder.append("ello,")
    stringBuilder.append("I am ")
    stringBuilder.append("Danny")
    println(stringBuilder)
    return 0
}
```

### 2.8  类的定义和封装
#### 2.8.1 类的定义
**类也必须定义在仓颉文件的顶层，同时，类里也分为实例成员和静态成员，类的构造函数也分为普通构造函数和主构造函数（主构造函数只能有一个）,这些性质与结构体相同**

```typescript
class Car {
    let carName: String
    let carType: String
    var carPrice: Float64
 
    // 构造函数
    init(carName: String, carType: String, carPrice:Float64) {
        this.carName = carName
        this.carType = carType
        this.carPrice = carPrice
    }
}
 
main () {
    let car = Car("XiaoMi","Yu7", 40)
    println(car.carName)
    println(car.carType)
    println(car.carPrice)
}
```

**<font style="background-color:#f3bb2f;">类的构造与函数完全相同，不再赘述</font>**

类与结构体的区别：

+ 类是引用类型、结构体是值类型（具体解释参考“结构体”章节）；
+ 类具有封装、继承、多态的三大特性（后面给大家详细介绍），而结构体不能继承，
+ 类及其成员支持所有访问控制，而结构体支持所有访问控制但是结构体成员不支持protected（被保护的）

#### 2.8.2 类的封装
在类外直接使用修改成员变量的操作不是安全的操作，我们并不希望外部能够任意访问类内部的成员变量，于是我们通过访问控制修饰符对类进行封装。

> **注意：prop修饰符，用于定义成员属性，mut修饰符修饰的成员属性，既可以读取，也可以被赋值，且必须包含get和set方法，没有被mut修饰的成员属性，只可以读取，不能被赋值。**
>

```typescript

 
class Car {
    // 主构造函数
    public Car(private var _carName: String, private var _carType: String, private var _carPrice: Float64) {
        if (this._carPrice > 0.0) {
            this._carPrice = _carPrice
        } else {
            println("汽车的价格非法输入")
        }
    }
 
    /*
    * 被private修饰的成员变量，必须使用getter或setter
    */
    mut prop carName: String {
        get () {
            _carName
        }
        set (_carName) {
            this._carName = _carName
        }
    }
 
    mut prop carType: String {
        get () {
            _carType
        }
        set (_carType) {
            this._carType = _carType
        }
    }
 
    mut prop carPrice: Float64 {
        get () {
            _carPrice
        }
        set (_carPrice) {
            if (_carPrice > 0.0) {
                this._carPrice = _carPrice
            } else {
                println("汽车的价格非法输入")
            }
        }
    }
}
 
main () {
    let car1 = Car("byd","纯电", 0.0)
    println(car1.carName)
    println(car1.carType)
    println(car1.carPrice)
 
    let car2 = Car("问界","纯电", 1.0)
    println(car2.carName)
    println(car2.carType)
    println(car2.carPrice)
}
```

### 2.9 类的继承
#### 2.9.1 继承的定义
在仓颉中，支持单继承，**不支持多继承**。通过继承，继承的类被称为子类或派生类，被继承的类称为父类、基类或者超类。继承时，子类除了父类的构造函数和private修饰的成员不能继承外，其他成员都可以使用。

> 父类加上open关键字
>
> 子类使用<:表示继承自
>
> 一个类最多可以有一个直接父类，但是可以有多个间接父类（祖先类）。
>

```typescript

 
// 父类加上open关键字
open class Animal {
    public Animal(public var name: String) {}
 
    public func say() {
        println("${name}: 在叫")
    }
 
    public func run() {
        println("${name}: 在跑")
    }
}
 
class Beer <: Animal {
    // 父类的构造函数不能被继承，使用时需要用super来调用父类的构造函数
    init(name: String) {
        super(name)
    }
    // 继承后的类，可以使用父类的除构造函数和private成员外的所有成员。
}
 
main () {
    let beer = Beer("灰熊")
    println(beer.name)
    beer.say()
    beer.run()
}
```

#### 2.9.2 重写和重定义
**当父类的成员不适合子类时，子类可以重新实现这些成员和增加成员。实例成员的重新实现叫做重写，静态成员的重新实现叫做重定义。**

> 重写需要加上override修饰符
>

```typescript

 
// 父类加上open关键字
open class Animal {
    public Animal(public var name: String) {}
 
    // 与类一样，加上open修饰符
    open public func say() {
        println("${name}: 在叫")
    }
 
    public func run() {
        println("${name}: 在跑")
    }
}
 
open class Beer <: Animal {
    // 父类的构造函数不能被继承，使用时需要用super来调用父类的构造函数
    init(name: String) {
        super(name)
    }
    // 继承后的类，可以使用父类的除构造函数和private成员外的所有成员。
}
 
class NortheastBeer <: Beer {
    init(name: String) {
        super(name)
    }
 
    // 重写的方法，前面加上override修饰符
    override public func say() {
        println("${name}: 熊二快看,是光头强")
    }
}
 
main () {
    let animal = Animal("动物")
    animal.say()
    animal.run()
    let b = Beer("熊")
    b.say()
    b.run()
    let beer = NortheastBeer("熊大")
    beer.say()
    beer.run()
}
```

> 静态成员的重定义
>

```typescript

 
// 父类加上open关键字
open class Animal {
    public Animal(public var name: String) {}
    
    // 静态函数
    public static func setType(t: String) {
        println("${t}")
    }
}
 
open class Beer <: Animal {
    // 父类的构造函数不能被继承，使用时需要用super来调用父类的构造函数
    init(name: String) {
        super(name)
    }
    // 继承后的类，可以使用父类的除构造函数和private成员外的所有成员。
 
    redef public static func setType(t: String) {
        println("我是: ${t}类")
    }
}
 
main () {
    Animal.setType("动物")
    Beer.setType("熊")
}
```

> **注意：子类中重写或者重定义时，函数名不变，函数返回值类型不变或是原类型的子类型，函数的形式参数不变，控制权限不能更严格，可以更宽松。**
>

### 2.10 类的多态
#### 2.10.1 多态的定义
多态是同一个引用类型的变量在访问同一个实例成员时表现出不同的行为。

> **当子类继承了父类并重写了父类的实例成员时，如果把子类对象赋值给父类类型的变量，那么该变量访问的就是被重写的子类实例成员，这样就构成了多态。**
>
> 孩子继承了爸爸的蓝色眼睛形状，但是孩子眼睛有点泛紫，这就是多态
>

```typescript

 
// 父类加上open关键字
open class Animal {
    public Animal(public var name: String) {}
 
    // 与类一样，加上open修饰符
    open public func say() {
        println("${name}: 在叫")
    }
 
    public func run() {
        println("${name}: 在跑")
    }
}
 
open class Beer <: Animal {
    // 父类的构造函数不能被继承，使用时需要用super来调用父类的构造函数
    init(name: String) {
        super(name)
    }
    // 继承后的类，可以使用父类的除构造函数和private成员外的所有成员。
    override public func say() {
        println("${name}: 在怒吼")
    }
}
 
main () {
    let animal = Animal("动物")
    animal.say()
 
    let beer = Beer("熊")
    beer.say()
    
    // 向上转型
    let whiteBeer: Animal = Beer("白熊")
    whiteBeer.say()
}
```

#### 2.10.2 组合
组合就是一个类可以将其他类的对象作为变量，进而实现了代码复用。

```typescript

 
class Car {
    Car(let name: String, let price: Float64){}
}
 
class Person {
    Person(let name: String, let car: Car) {}
}
 
main () {
    let man = Person("钝子生", Car("问界", 31.99))
    println("我叫${man.name}, 我有一辆${man.car.name}")
    let car = Car("享界", 33.1)
    let woman = Person("尖子生", car)
    println("我叫${woman.name}, 我有一辆${woman.car.name},售价: ${woman.car.price}")
}
```

#### 2.11 抽象类
定义类的时候，会预想到这个类会有函数，可是却不知道这个函数究竟该实现什么样的功能，比如：定义一个图形类，这个图形类有很多子类，矩形，三角形，圆，而图形类里面有计算图形周长和面积的函数，而到了子类中，因为不同图形的计算公式不同，为此在图形类中的函数就不明确该怎么写了，因此，为了解决这个问题，就诞生了抽象类。

> **在抽象类中，抽象类不能被实例化，可以定义抽象变量和抽象函数。**
>
> + 抽象类 abstract修饰
> + 抽象属性 访问控制必须是 public 或者 默认
> + <:表示实现这个抽象类
> + 实现抽象方法也是要重写Override
>

> 注意：
>
> + 抽象类通过abstract修饰符修饰；
> + 抽象变量在父类中可以不用赋值，但必须加上数据类型；
> + 抽象函数在父类中没有函数体，但必须有函数返回值类型；
> + 抽象变量和抽象函数必须是实例成员，不能是静态成员，但是抽象类里允许定义静态成员；
> + 实现抽象函数的规则和重写（重定义）函数规则相同；
> + 抽象类也支持间接继承；
> + 实现抽象变量的规则就是子类型的修饰符必须和父类保持一致。
>

```typescript

 
/*
* 抽象类 abstract修饰
*/
abstract class Shape {
    // 抽象属性 访问控制必须是 public 或者 默认
    public mut prop internalAngleSum: Float64
 
    /*
    * 构造函数
    */
    public Shape(private var _length: Float64) {}
 
 
    /*
    * 抽象函数 计算面积
    */
    public func area(): Float64
}
 
class Rectangle <: Shape {
    public Rectangle(private var _length: Float64, private var _internalAngleSum: Float64) {
        super(_length)
    }
 
    // 实现抽象属性
    public mut prop internalAngleSum: Float64 {
        get() {
            _internalAngleSum
        }
 
        set(_internalAngleSum) {
            this._internalAngleSum = _internalAngleSum
        }
    }
 
    // 实现抽象方法 
    override public func area() {
        return this._length * this._length
    }
}
 
class Circle <: Shape {
    public Circle(private var _length: Float64, private var _internalAngleSum: Float64) {
        super(_length)
    }
 
    // 实现抽象属性
    public mut prop internalAngleSum: Float64 {
        get() {
            _internalAngleSum
        }
 
        set(_internalAngleSum) {
            this._internalAngleSum = _internalAngleSum
        }
    }
 
    // 实现抽象方法 
    override public func area() {
        return this._length ** 2 * 3.14 
    }
}
 
main () {
    let jx = Rectangle(3.0, 360.0)
    println(jx.area())
    let yuan = Circle(3.0, 360.0)
    println(yuan.area())
}
```

#### **抽象类的特殊继承**
继承抽象类的子类，如果没有实现父类中的抽象变量和抽象函数，那么在类中依旧可以定义为抽象变量或者抽象函数，前提这个子类也得是抽象类。

```typescript

 
/*
* 抽象类 abstract修饰
*/
abstract class Shape {
    // 抽象属性 访问控制必须是 public 或者 默认
    public mut prop internalAngleSum: Float64
 
    /*
    * 构造函数
    */
    public Shape(private var _length: Float64) {}
 
 
    /*
    * 抽象函数 计算面积
    */
    public func area(): Float64
}
 
class Rectangle <: Shape {
    public Rectangle(private var _length: Float64, private var _internalAngleSum: Float64) {
        super(_length)
    }
 
    // 实现抽象属性
    public mut prop internalAngleSum: Float64 {
        get() {
            _internalAngleSum
        }
 
        set(_internalAngleSum) {
            this._internalAngleSum = _internalAngleSum
        }
    }
 
    // 实现抽象方法 
    override public func area() {
        return this._length * this._length
    }
}
 
/*
* 依旧是抽象类
*/
abstract class Circle <: Shape {
    public Circle(private var _length: Float64, private var _internalAngleSum: Float64) {
        super(_length)
    }
 
    // 抽象属性 访问控制必须是 public 或者 默认
    public mut prop internalAngleSum: Float64
 
    /*
    * 抽象函数 计算面积
    */
    public func area(): Float64
}
 
main () {
    let jx = Rectangle(3.0, 360.0)
    println(jx.area())
}
```

> 一个抽象类被**sealed**修饰了，那么这个抽象类只能在本包内去继承，sealed修饰符只会影响到当前类，不会影响子类的访问控制。
>

抽象类的意义：

+ 实现代码复用，抽象类中明确好的函数和变量，写一次，就可以在子类中去使用，实现代码复用；
+ 强制子类结构，确保了子类无论如何变化，其基本特性都和父类一致；
+ 静止实例化，抽象类更像是一个类的模板而不是用于创建使用；
+ 增强扩展性，使得代码设计更加模块化。

### 2.11 接口
> 接口是比**抽象类**还要抽象的类型，使用**interface**标识符。
>
> + 接口中所有成员都必须是公共的
> + 可以省略不写public修饰符
> + 和抽象类一样，接口不能被实例化，接口也支持间接实现
> + 与抽象类不同，接口支持多实现，接口也没有构造方法。
>

**注意：在接口中，因为默认访问控制都是公共的，因此可以不写public，但是在实现类里，必须写public。在实现类中的函数和变量的实现规则与抽象类中的规则一致。**

```typescript

 
public interface Shape {
    // 抽象变量
    mut prop internalAngleSum: Float64
    // 抽象函数
    func area(): Float64
}
 
public interface Rectangle {
    // 抽象函数
    func girth(): Float64
}
 
/*
* 正方形类
*/
public class Square <: Shape & Rectangle {
    public Square(private var _length: Float64, private var _internalAngleSum: Float64) {
    }
 
    // 实现抽象变量
    public mut prop internalAngleSum: Float64 {
        get() {
            _internalAngleSum
        }
 
        set(_internalAngleSum) {
            this._internalAngleSum = _internalAngleSum
        }
    }
 
    // 实现抽象函数
    public override func area() {
        return this._length * this._length
    }
 
    // 实现抽象函数
    public override func girth() {
        return this._length * 4.0
    }
} 
 
main () {
    let square = Square(5.0, 360.0)
    println(square.area())
    println(square.girth())
}
```

#### **接口的默认实现**
仓颉中的接口，提供了默认实现，当某个接口的成员有默认实现时，实现这个接口的实现类就可以根据自己的需要，去实现该成员了。如果符合需要，就不用再去实现，如果不符合需要，再去重新实现。

```typescript

 
/*
* 接口
*/
public interface Shape {
    // 抽象变量
    mut prop length: Float64
 
    // 默认实现
    func area(): Float64 {
        return this.length * this.length
    }
}
 
/*
* 正方形类
*/
public class Square <: Shape {
    public Square(private var _length: Float64) {}
 
    // 实现抽象变量
    public mut prop length: Float64 {
        get() {
            _length
        }
 
        set(_length) {
            this._length = _length
        }
    }
}
 
/*
* 圆
*/
public class Circle <: Shape {
    public Circle(private var _length: Float64) {}
    // 实现抽象变量
    public mut prop length: Float64 {
        get() {
            _length
        }
 
        set(_length) {
            this._length = _length
        }
    }
 
    /*
    * 实现默认实现的函数
    */
    public override func area() {
        return this._length ** 2 * 3.14
    }
}
 
main () {
    let square = Square(5.0)
    println(square.area())
    let circle: Shape = Circle(5.0)
    println(circle.area())
}
```

> 注意：
>
> 不论是类、抽象类还是接口，它们的基本特性都是一致的，接口和抽象类也支持向上转型。
>
> **Any**类型是任何类型的父类型，即：所有类型都是Any的子类型。
>

### 2.12 枚举类型
枚举类型是仓颉中，最后一种自定义类型，用**enum标识符**表示。

```typescript
/*
* 枚举
*/
enum Color {
    // 构造器 构造器允许重名
    Red | Red(Int64) | Blue(Int64) | Green(Int64)
    
    //成员函数
    func show() {
        println("展示")
    }

}
 
main () {
    var color = Color.Red
    color = Color.Red(0xFF0000)
}
```

> 注意：
>
> + enum类型中可以有有参构造器，也可以有无参构造器；
> + enum类型中构造器的名称可以重名；
> + enum类型也有访问控制；
> + enum类型中也可以定义成员变量和成员函数。
>

#### enum值的模式匹配
```typescript
/*
* 枚举
*/
enum Color {
    // 构造器 构造器允许重名
    Red | Red(String) | Blue(String)
}
 
func matchColor(color: Color) {
    match (color) {
        case Red => println("Red的无参构造器")
        case Red(red) => println("Red的有参构造器: ${red}")
        case Blue(blue) => println("Blue的有参构造器: ${blue}")
    }
}
 
main () {
    matchColor(Color.Red)
    matchColor(Color.Red("红色"))
    matchColor(Color.Red("蓝色"))
}
```

### 2.13 Option类型
**仓颉通过Option类型解决了空指针问题，实现了编码即安全，极大的提高了代码的健壮性。Option类型通过None值来表示空指针。**

Option<T>类型的具体类型根据泛型来决定

**Option类型包含两个构造器，Some和None,Some携带一个参数T，表示有值，None不携带参数，表示没有值，当程序中某个类型可能有值或没有值的时候，就用Option类型。**

#### Option类型的创建
```typescript
main () {
    // 第一种赋值方式
    let optNumber: Option<Int64> = Option<Int64>.Some(18)
    println(optNumber)
 
    // 第二种赋值方式
    let optFlag: Option<Bool> = Some(true)
    println(optFlag)
    // 第三种赋值方式
 
    let optString: Option<String> = "你好, 仓颉"
    println(optString)
    
    // 赋值为None
    let optFloat: Option<Float64> = Option<Float64>.None
    println(optFloat)
 
    // 赋值为None
    let optChar: Option<Rune> = None
    println(optChar)
 
    // 赋值为None
    let optUInt: Option<UInt64> = None<UInt64>
    println(optUInt)
}
```

> 当Option类型用于显式的使用时，可以将Option<T>简写成“?T”
>
> Option<Int64> ->  ?Int64
>
> 以上代码可以写成
>

```typescript
main () {
    // 第一种赋值方式
    let optNumber: ?Int64 = Option<Int64>.Some(18)
    println(optNumber)
 
    // 第二种赋值方式
    let optFlag: ?Bool = Some(true)
    println(optFlag)
    // 第三种赋值方式
 
    let optString: ?String = "你好, 仓颉"
    println(optString)
    
    // 赋值为None
    let optFloat: ?Float64 = Option<Float64>.None
    println(optFloat)
 
    // 赋值为None
    let optChar: ?Rune = None
    println(optChar)
 
    // 赋值为None
    let optUInt: ?UInt64 = None<UInt64>
    println(optUInt)
 
    let opt: ?Int64 = 114514
    println(fun1(opt))
    println(fun2(opt))
}
 
/*
在函数里使用
*/
func fun1(opt: Option<Int64>): Option<Int64> {
    return opt
}
 
func fun2(opt: ?Int64): ?Int64 {
    return opt
}
```

#### Option类型的值
通过println函数可以把Option的值输出到控制台，但是输出的结果，如果没有值就是None，有值，都被Some()包裹，无法直接使用这个值，这个时候就需要想办法把值取出来，仓颉为我们提供了四种方法，来取值。

```typescript
main () {
    // 第一种赋值方式
    let optNumber: ?Int64 = Option<Int64>.Some(5201314)
    println(optNumber)
 
    // 方法1 match表达式
    println("\nmatch表达式")
    match(optNumber) {
        case None => println("您没有赋值")
        case Some(value) => println(value)
    }
 
    
    // 方法2 getOrThrow函数
    println("\ngetOrThrow函数")
    if(optNumber.isSome()) {
        println(optNumber.getOrThrow())
    } else {
        println("您没有赋值")
    }
 
    // 方法3 ??操作符
    // ??表示 ??左边有值Some(value)，就会返回value的值，如果没值，就返回??右边的值
    println("\n??操作符")
    var value = optNumber ?? 0
    println(value)
 
}
```

第四种方法，适合在类、结构体这种需要靠“.”来使用变量的情况。这里用类来举例

```plain
class Girl {
    public Girl(private var _name: String, private var _age: Int64) {}
 
    mut prop name: String {
        get() {
            return this._name
        }
        set(_name) {
            this._name = _name
        }
    }
 
    mut prop age: Int64 {
        get() {
            return this._age
        }
        set(_age) {
            this._age = _age
        }
    }
}
 
main () {
    // 方法4 ?操作符 
    var opt: Option<Girl> = Some(Girl("女神", 24))
    println("小明的女神信息")
    match(opt?.name) {
        case Some(name) => println("姓名: ${name}")
        case None => println("想peach, 你也配有女神")
    }
    match(opt?.age) {
        case Some(age) => println("年龄: ${age}")
        case None => println("想peach, 你也配有女神")
    } 
}
```

#### as的类型转换
向上转型时，如果子类用父类型，那么子类型中的特有成员就会使用不了。而想要访问特有成员时，需要使用as关键字，进行向下转型。

```typescript

 
open class Father {
    open public func fun1() {
        println("父类fun1")
    }
}
 
class Son <: Father {
    override public func fun1() {
        println("子类fun1")
    }
 
    public func fun2() {
        println("子类独有fun2")
    }
}
 
main () {
    // 向上转型
    let son: Father = Son()
    son.fun1()
    // 使用不了fun2函数
}
```

**向下转型：把父类型变量转成子类型。**

```typescript

 
open class Father {
    open public func fun1() {
        println("父类fun1")
    }
}
 
class Son <: Father {
    override public func fun1() {
        println("子类fun1")
    }
 
    public func fun2() {
        println("子类独有fun2")
    }
}
 
main () {
    // 向上转型
    let son: Father = Son()
    // 向下转型
    let newSon = (son as Son)
    if (newSon.isSome()) {
        newSon.getOrThrow().fun2()
    } else {
        println("解构失败")
    }
}
```

代码中使用isSome()函数判断是否有值，也可以使用is修饰符。

```typescript

 
open class Father {
    open public func fun1() {
        println("父类fun1")
    }
}
 
class Son <: Father {
    override public func fun1() {
        println("子类fun1")
    }
 
    public func fun2() {
        println("子类独有fun2")
    }
}
 
main () {
    // 向上转型
    let son: Father = Son()
    // 向下转型
    let newSon = (son as Son)
    // is 修饰符
    if (newSon is Option<Son>) {
        newSon.getOrThrow().fun2()
    } else {
        println("解构失败")
    }
}
```

### 2.14 match表达式
#### 2.14.1 match表达式
match表达式包含：有待匹配值的match表达式和无待匹配值的match表达式

有待匹配值的match表达式

```typescript
main () {
    // 角度 
    let angle = 45
    match (angle) {
        case _ where angle >= 0 && angle < 90 => println("${angle}是锐角")
        case _ where angle >90 && angle < 180 => println("${angle}是钝角")
        case _ where angle == 180 => println("${angle}是平角")
        case _ where angle == 360 => println("${angle}是周角")
        case _ => println("${angle}是直角")
    }
}
```

**注意：where表达式具有补充条件的作用，在循环结构里也可以使用。**

无待匹配值的match表达式

```typescript
main () {
    let score = 79
    match {
        case score < 0 || score > 100 => println("${score}是非法成绩")
        case score < 60 => println("${score}是不及格")
        case score < 70 => println("${score}是及格")
        case score < 80 => println("${score}是中等")
        case score < 90 => println("${score}是良好")
        case _ => println("${score}是优秀")
    }
}
```

#### 2.14.2 match表达式的类型
任何表达式都是有值的，match表达式类型是Unit，值为()

```typescript
main () {
    let score = 79
    var str = ""
    let result = match {
        case score < 0 || score > 100 => str = "${score}是非法成绩"
        case score < 60 => str = "${score}是不及格"
        case score < 70 => str = "${score}是及格"
        case score < 80 => str = "${score}是中等"
        case score < 90 => str = "${score}是良好"
        case _ => str = "${score}是优秀"
    }
    println("${str}")
    println("${result}")
}
```

match表达式支持模式匹配

```typescript
main () {
    let score = 79
    let result: String = match {
        case score < 0 || score > 100 => "${score}是非法成绩"
        case score < 60 => "${score}是不及格"
        case score < 70 => "${score}是及格"
        case score < 80 => "${score}是中等"
        case score < 90 => "${score}是良好"
        case _ => "${score}是优秀"
    }
    println("${result}")
}
```

### 2.15 模式匹配
#### 2.15.1 通配符模式
通配符模式使用下划线（_）表示，可以匹配任何值，在match表达式中，已经使用过了通配符表达式了，这里就不做过多介绍。

```typescript
func wildcardPat(x: Int64) {
    match (x) {
        case _ => "_"
    }
}
```

#### 2.15.2 常量模式
常量模式包含：整型常量、浮点型常量、字符型常量、字符串常量、布尔常量、Unit常量。

```typescript
func constPat(x: Int64) {
    match (x) {
        case 1 => "one"
        case 2 => "two"
        case _ => "_"
    }
}
```

#### 2.15.3 绑定模式
```typescript
func varPat(x: Int64) {
    match (x) {
        case a => "x = ${a}"
    }
}
```

#### 2.15.4 类型模式
```typescript
interface I {}
open class Base <: I {}
class Derived <: Base {}

func typePat(x: I) {
    match (x) {
        case a: Derived => "Derived"
        case b: Base => "Base"
        case _ => "Other"
    }
}
```

#### 2.15.5 元组模式
```typescript
func tuplePat(x: (Int64, Int64)) {
    match (x) {
        case (1, 2) => "(1, 2)"
        case (a, 2) => "(${a}, 2)"
        case (a, b) => "(${a}, ${b})"
    }
}
```

#### 2.15.6 枚举模式
```typescript
enum E1 {
    A(Int64)
}


enum E2 {
    B(Int64) | C(Int64)
}


func enumPat1(x: E1) {
    match (x) {
        case A(1) => "A(1)"
        case A(a) => "A(${a})"
    }
}


func enumPat2(x: E2) {
    match (x) {
        case B(b) => "B(${b})"
        case C(c) => "C(${c})"
    }
}
```

### 2.15 B 模式匹配扩展
#### 模式的 Refutability
模式可以分为两类：refutable 模式和 irrefutable 模式：

#### **Refutability**
+ 英文意为**可反驳性**，我认为你可以直接记忆为**可能不匹配性：**  
当一个模式有可能和待匹配值不匹配时，称此模式为 refutable 模式

#### **Irrefutability：**
+ **无可辩驳的**，建议记忆为，**绝对匹配的**  
当一个模式总是可以和待匹配值匹配时，称此模式为 irrefutable 模式

前面介绍的6种模式的refutability如下：

+ **常量模式是 refutable 模式**
+ **通配符模式是 irrefutable 模式**
+ **绑定模式是 irrefutable 模式**
+ **Tuple 模式是 irrefutable 模式**
+ **类型模式是 refutable 模式**
+ **enum 模式是 irrefutable 模式**

### 2.16 嵌套函数
## 3.进阶语法
### 3.1 函数调用语法糖
#### **3.1.1 Lambda表达式**
##### 什么是lambda表达式：
<font style="background-color:#f3bb2f;">本质是没有没名字的函数，满足函数的所有特性</font> 

形式：

$ { \{para1：T1，papa2：T2, ...,paran:Tn =&gt; expressions     		  |  declarations \}} $

规则：

+ `=>` 之前为参数列表，多个参数之间使用 `,` 分隔
+ 每个参数名和参数类型之间使用 `:` 分隔
+ `=>` 之前也可以没有参数
+ `=>` 之后为 lambda 表达式体，是一组表达式或声明序列
+ Lambda 表达式的参数名的作用域与函数的相同，为 lambda 表达式的函数体部分，其作用域级别可视为与 lambda 表达式的函数体内定义的变量等同。

```typescript
let f1 = { a: Int64, b: Int64 => a + b }

var display = { =>   // Parameterless lambda expression.
    println("Hello")
    println("World")
}
```

Lambda 表达式不管有没有参数，都不可以省略 `=>` ，除非其作为[尾随 lambda](https://developer.huawei.com/consumer/cn/doc/cangjie-guides-V5/function_call_desugar-V5#尾随-lambda)。例如：

```typescript
var display = { => println("Hello") }

func f2(lam: () -> Unit) {}
let f2Res = f2 { println("World") } // OK to omit the =>
```

##### **尾随Lambda**
尾随lambda可以使得函数调用如同语言内置语法一样，增加语言的可扩展性

当函数最后一个形参是函数类型，并且函数调用对应的实参是 lambda 时，可以使用尾随 lambda 语法，将 lambda 放在函数调用的尾部，圆括号外面。

```typescript
func deliverSpeech(content: String, speakerHandler: (String) -> Unit) { // speaker是第二个参数名，不要误解为lambda的函数名，lambda是匿名的
    speakerHandler(content)
}

main() {
    deliverSpeech("欢迎来到仓颉语言实践课堂！") { speech => // 使用尾随lambda调用
                                                //           ↑ 
                                                // 这个speech叫做回调参数，你可以理解为一个占位符，值就是"欢迎来到仓颉语言实践课堂！"
        println("我是演讲者Danny: ${speech}")
    }
    return 0
}
```

上面这个示例中 函数`deliverSpeech`第一个参数为演讲内容，第二个参数为**接收一个String类型返回Unit**的演讲者函数

main的调用中，第一个参数正常传入演讲内容，第二个参数利用lambda表达式传入演讲者名称

等价于如下调用：

```typescript
func deliverSpeech(content: String, speakerHandler: (String) -> Unit) {
    speakerHandler(content)
}

func announceSpeech(speech: String) {
    println("我是演讲者Danny: ${speech}")
}

main() {
    deliverSpeech("欢迎来到仓颉语言实践课堂！", announceSpeech)
    return 0
}
```

当函数调用有且只有一个 lambda 实参时，还可以省略 ()，只写 lambda：

```typescript
func f(fn: (Int64) -> Int64) { fn(1) }

main() {
    f { i => i * i }
}
```

#### **3.1.2 Flow 表达式**
**流操作符**包括两种：

+ 表示数据流向的中缀操作符 |> （称为 **pipeline**）
+ 表示函数组合的中缀操作符 ~> （称为 **composition**）

##### Pipeline 表达式
当需要对输入数据做一系列的处理时，可以使用 pipeline 表达式来简化描述

pipeline 表达式的语法形式如下：

```typescript
e1 |> e2
```

等价于如下形式的语法糖：

```typescript
let v = e1; 
e2(v) 
```

其中 `e2` 是函数类型的表达式，`e1` 的类型是 `e2` 的参数类型的子类型。

示例

```typescript
main() {
    let a = 1

    func add1(i: Int64): Int64 { return i + 1  }

    func add2(i: Int64): Int64 { return i + 2 }

    let result = a |> add1 |> add2
    
    println("result: ${result}") // 4
}
```

##### Composition 表达式
composition 表达式表示两个**单参函数**的组合

composition 表达式语法如下： 

```typescript
f ~> g
```

等价于如下形式： 

```typescript
{ x => g(f(x)) }
```

其中 `f`，`g` 均为只有一个参数的函数类型的表达式，`f` 和 `g` 组合，则要求 `f(x)` 的返回类型是 `g(...)` 的**参数类型的子类型**

示例 1：

```typescript
func f(x: Int64): Float64 {
    Float64(x)
}
func g(x: Float64): Float64 {
    x
}

var fg = f ~> g // The same as { x: Int64 => g(f(x)) }
```

示例 2：

```typescript
func f(x: Int64): Float64 {
    Float64(x)
}

let lambdaComp = ({x: Int64 => x}) ~> f // The same as { x: Int64 => f({x: Int64 => x}(x)) }
```

示例 3：

```typescript
func h1<T>(x: T): T { x }
func h2<T>(x: T): T { x }
var hh = h1<Int64> ~> h2<Int64> // The same as { x: Int64 => h2<Int64>(h1<Int64>(x)) }
```

> 注意
>
> 表达式 f ~> g 中，会先对 f 求值，然后对 g 求值，最后才会进行函数的组合。
>

另外，流操作符不能与无默认值的命名形参函数直接一同使用，这是因为无默认值的命名形参函数必须给出命名实参才可以调用。例如：

```typescript
func f(a!: Int64): Unit {}

var a = 1 |> f  // Error
```

如果需要使用，用户可以通过 lambda 表达式传入 f 函数的命名实参：

```typescript
func f(a!: Int64): Unit {}

var x = 1 |>  { x: Int64 => f(a: x) } // Ok
```

由于相同的原因，当 f 的参数有默认值时，直接与流运算符一起使用也是错误的，例如：

```typescript
func f(a!: Int64 = 2): Unit {}

var a = 1 |> f // Error
```

但是当命名形参都存在默认值时，不需要给出命名实参也可以调用该函数，函数仅需要传入非命名形参，那么这种函数是可以同流运算符一起使用的，例如：

```typescript
func f(a: Int64, b!: Int64 = 2): Unit {}

var a = 1 |> f  // Ok
```

当然，如果想要在调用 f 时，为参数 b 传入其他参数，那么也需要借助 lambda 表达式：

```typescript
func f(a: Int64, b!: Int64 = 2): Unit {}

var a = 1 |> {x: Int64 => f(x,  b: 3)}  // Ok
```

### 3.2 基础的输入输出流（I/O流）
#### 3.2.1 流
输入和输出数据的处理方式就叫流

stdio: **st**an**d**ard **i**nput and **o**utput 标准输入输出

<!-- 这是一张图片，ocr 内容为： -->
![](https://resource.duyiedu.com/yuanjin/202507121302745.png)<!-- 这是一张图片，ocr 内容为： -->
![](https://resource.duyiedu.com/yuanjin/202507121308463.png)

> 除了标准的输入和输出流外，还有一条独立的错误流（错误流也是输出流），专门输出程序运行时的错误信息，日志就是基于此输出错误信息到文件中的
>

这三条流是系统最底层的流，其他流类型都是基于此的封装

##### 控制台读写：
输入流：

```typescript
import std.env.*

func 读取单个字符() {
    print("请输入1个字符: ")
    let optChar = getStdIn().read()
    if (let Some(char) <- optChar) {
        println(char)
    }
    
    // 清理输入缓冲区中的回车符
    getStdIn().readln()  // 消耗掉剩余的回车符
    
    print("请输入多个字符: ")
    var optString: Option<String> = getStdIn().readln()
    println("你输入的是: ${optString.getOrThrow()}")
}
```

输出流：

```typescript
func 写入信息到控制台() {
    getStdOut().write("请输入信息:")
    var optStirng = getStdIn().readln()
    var string = optStirng.getOrThrow()
    getStdOut().writeln(string)
}
```

> `print()`是对输出流的上层封装
>

错误流：

```typescript
func 错误流使用演示() {
    // 标准输出流 - 正常信息
    getStdOut().write("标准输出: 这是正常信息")
    getStdOut().writeln(" (stdout)")
    
    // 错误流 - 错误信息  
    getStdErr().write("错误输出: 这是错误信息")
    getStdErr().writeln(" (stderr)")
    
    // 混合使用
    getStdOut().writeln("正常: 程序开始执行...")
    getStdErr().writeln("警告: 发现潜在问题！")
    getStdOut().writeln("正常: 继续执行...")
    getStdErr().writeln("错误: 执行失败！")
}
```

区别：

| 流类型 | 函数 | 用途 |
| :--- | :--- | :--- |
| 标准输入流 | getStdIn() | 接收用户输入 |
| 标准输出流 | getStdOut() | 输出正常信息和结果 |
| 错误流 | getStdErr() | 输出错误信息、警告、调试信息 |


 为什么要分离错误流？

1. 独立重定向 - 可以单独将错误信息重定向到文件
2. 日志管理 - 正常输出和错误信息可以分别处理
3. 调试便利 - 错误信息不会与正常输出混合
4. 系统标准 - 符合 Unix/Linux 系统的标准流概念

#### 3.2.2 文件操作
有了数据流我们就可以对文件进行各种操作

学习文件操作必须简要学习一下 `File` 类

class File

```typescript
public class File <: Resource & IOStream & Seekable {
    public init(path: Path, openOption: OpenOption)
    public init(path: String, openOption: OpenOption)
}
```

两种构造方式，一种path对象，另外一种是对应路径的字符串

注意：

两种方式都不能使用非法路径，在输入路径时，应该避免使用非法字符，确保路径的合法性，以便正确地访问目标文件或目录。

> + 路径中包含非法字符，例如空格、制表符、换行符等；
> + 路径中包含不合法的字符，例如特殊字符、控制字符等；
> + 路径中包含不存在的目录或文件；
> + 路径中包含无法访问的目录或文件，例如权限不足或被锁定等。
>

其中 `OpenOption` 有六种枚举模式 

```typescript
public enum OpenOption {
    | Append					// 文件尾续写模式，默认具有Write权限
    | Create(Bool)				// 创建新的文件，默认Write，Bool参数决定这个新文件是否具有Read权限
    | CreateOrAppend			// 如果文件不存在，则使用 Create；否则使用 Append。
    | CreateOrTruncate(Bool)	// 如果文件不存在，则使用 Create；否则使用 Truncate。
    | Open(Bool, Bool)			// 打开文件（不存在会报错） 第一个参数指定是否具有 Read 权限，第二个参数指定是否具有 Write 权限。
    | Truncate(Bool)			// 覆盖文件
}
```

> FIle对象的更多操作方法可以查阅[文档-仓颉编程语言官网](https://cangjie-lang.cn/docs?url=%2F1.0.3%2Flibs%2Fstd%2Ffs%2Ffs_package_api%2Ffs_package_classes.html)
>

##### 示例1：读取文件
```typescript
import std.fs.*
import std.io.*
    
func 基础文件操作() {
    let path = Path("./src/test/testFile.txt")
    // File.readFrom读取的是字符，也就是Unicode码，不是原始字符
    let byteArray = File.readFrom(path)
    println("Unicode码: ${byteArray}")
    // 使用String.fromUtf8将Unicode码转成原始数据
    let str = String.fromUtf8(byteArray)
    println("原始数据: ${str}")
}
```

1.构建一个Path对象和一个字节数组

2.利用 `File.readFrom` 读取path中的Unicode码

3.通过 `String.fromUtf8` 获取到原始的文本数据

##### 示例2：写入文件
写入文件有两种种方式续写、覆写

续写文件：

```typescript
import std.fs.*
import std.io.*
    
func 续写文件() {
    var file = File(Path("./src/test/WriteIn.txt"), Append)
    let data = "I am Danny!"
    let byteArray = data.toArray()
    file.write(byteArray)
    println("写入数据: ${String.fromUtf8(byteArray)}")

    println("全文件内容如下:")
    println("${String.fromUtf8(File.readFrom(Path("./src/test/WriteIn.txt")))}")
    file.close()
    println("文件已关闭")
}
```

> 创建的 [File](https://docs.cangjie-lang.cn/docs/1.0.3/libs/std/fs/fs_package_api/fs_package_classes.html#class-file) 对象会默认打开对应的文件，当使用结束后需要及时调用 [close](https://docs.cangjie-lang.cn/docs/1.0.3/libs/std/fs/fs_package_api/fs_package_classes.html#func-close) 函数关闭文件，否则会造成资源泄露。
>

覆写文件：

```typescript
import std.fs.*
import std.io.*

func writeTo覆写函数测试() {
    let path1 = Path("./src/test/testFile.txt")
    let path2 = Path("./src/test/testFile2.txt")
    let data = File.readFrom(path1)
    File.writeTo(path2, data)
}
```

##### 示例3：光标操作
基于File对象的方法我们也可以指定光标位置对文件进行操作

```typescript
func 特定位置读取文件() {

    var file = File(Path("./src/test/testFile.txt"), Read)
    println("刚打开时:")
    println("length: ${file.length}")
    println("position: ${file.position}")
    println("remainLength: ${file.remainLength}")
 
    // 读取前4个字节数据
    let byteArray = Array<Byte>(4, repeat: 0)
    println("已经读取字节数: ${file.read(byteArray)}")
    println("已经读取数据: ${String.fromUtf8(byteArray)}")
 
    // 读取4字节后的position和remainLength
    println("读取4字节后:")
    println("position: ${file.position}")
    println("remainLength: ${file.remainLength}")
 
    // 读取后面剩下的数据
    println("读取后面剩下的数据:")
    println("剩下的数据: ${String.fromUtf8(readToEnd(file))}")
    println("position: ${file.position}")
    println("remainLength: ${file.remainLength}")
    
    // 调用seek函数，将光标移动到一开始
    println("移动光标的位置: ${file.seek(SeekPosition.Begin(0))}")
    println("所有数据: ${String.fromUtf8(readToEnd(file))}")
    println("position: ${file.position}")
    println("remainLength: ${file.remainLength}")
 
    // 释放资源
    file.close()
    println("文件已关闭")
}
```

### 3.3 其他流类型
#### 3.3.1 缓冲流
缓冲流（可以对比Java 中的 `BufferedInputStream`、`BufferedOutputStream`、`BufferedReader`、`BufferedWriter` 等）是**对底层 I/O 流的封装**，其核心作用是**减少对底层设备（如磁盘、网络）的实际读写次数**，通过在内存中维护一个缓冲区（buffer）来提高 I/O 效率。

+ 缓冲流的写入是**程序写入到内存缓冲区**
+ 缓冲流的输出是**将内存缓冲区的数据写入到目标设备（如磁盘）**

##### 缓冲流的操作：
**注意：缓冲流是包装流，只需要关闭BufferedOutputStream对象，就把文件对象一起关闭了。**

读取：

```typescript
import std.io.*
import std.fs.*
 
func 使用缓冲区读取文件() {
    try(
        file = File(Path("./src/test/testFile3.txt"), Read),
        // 创建缓冲区为4字节的实例
        // 缓冲区越大 = 磁盘访问次数越少 = 性能越好
        // 缓冲区越小 = 内存占用越少 = 但磁盘访问频繁
        bufferedInputStream = BufferedInputStream(file, 4)
    ){
        let byteArray = Array<Byte>(10, repeat: 0)
        bufferedInputStream.read(byteArray)
        println(String.fromUtf8(byteArray))
    }
}
```

写入：

```typescript
import std.io.*
import std.fs.*

func 使用缓冲区写入文件() {
    let file = File(Path("./src/test/bufferWriteIn.txt"), Append)
    let bufferedOutputStream = BufferedOutputStream(file, 4)
    bufferedOutputStream.write("使用缓冲区写入文件".toArray())
    // 将缓冲区的数据，刷新绑定到输出流
    bufferedOutputStream.flush()
    // 关闭资源
    bufferedOutputStream.close()
}
```

#### 3.3.2 字符串流（StringReader 与 StringWriter）
`StringReader` 和 `StringWriter` 是**基于内存的字符流**，用于在**不涉及实际文件或网络**的情况下，以流的方式读写字符串数据。它们操作的是**字符（char）而非字节（byte）**，属于**字符流（Character Stream）** 范畴。

+ `StringReader`：将一个**已有字符串**作为输入源，提供类似文件读取的流式接口（如 `read()`, `readln()`）。
+ `StringWriter`：将写入的字符**暂存于内存缓冲区**，最终可导出为字符串，**不直接操作文件**。

> ⚠️ **重要澄清**：  
`StringReader` **不是**将“字节流转成字符”，而是**直接读取字符串内容**；   
`StringWriter` **不是**将“字符转成字节流输出到文件”，而是**在内存中累积字符**，若需写入文件，仍需配合 `File` 或 `FileWriter` 等流。
>

##### 常见误区纠正
+ ❌ 错误理解：   
“`StringReader` 读取文件字节并转为字符”   
“`StringWriter` 直接向文件写入字符”
+ ✅ 正确理解：   
`StringReader` 和 `StringWriter` **仅在内存中操作字符串**，**不直接绑定文件**。若要读写文件，应使用 `FileReader` / `FileWriter` 或配合 `File` + 字符编码转换。

---

##### 正确使用示例
###### 1. 使用 `StringReader` 读取内存中的字符串（非文件）
```typescript
import std.io.*

func 使用StringReader() {
    let content = "Hello from StringReader!"
    try(
        reader = StringReader(content)
    ) {
        let line = reader.readln()
        println(line.getOrThrow()) // 输出: Hello from StringReader!
    }
}
```

> 📌 注意：`StringReader` 的构造参数是 **字符串（String）**，不是 `File` 对象。
>

---

###### 2. 使用 `StringWriter` 在内存中构建字符串
```typescript
import std.io.*

func 使用StringWriter() {
    try(
        writer = StringWriter()
    ) {
        writer.writeln("第一行")
        writer.writeln("第二行：我是StringWriter流")
        // 获取最终结果
        let result = writer.toString()
        println(result)
    }
}
```

> 📌 `StringWriter` **不会自动写入文件**。若需保存到文件，需额外操作：
>

```typescript
import std.io.*
import std.fs.*

func StringWriter写入文件() {
    let writer = StringWriter()
    writer.writeln("我是StringWriter流")

    // 手动将内容写入文件
    let file = File(Path("./src/dir3.txt"), Write)
    file.write(writer.toString().toUtf8())
    file.close()
}
```

---

##### 关于原始代码的修正说明
原始代码中存在概念错误：

```typescript
// ❌ 错误：StringReader 不能直接包装 File
stringReader = StringReader(file)

// ❌ 错误：StringWriter 不能直接绑定 File 进行写入
stringWriter = StringWriter(file)
```

##### 总结
| 流类型 | 输入源 / 输出目标 | 是否操作文件 | 是否基于内存 |
| --- | --- | --- | --- |
| `StringReader` | 字符串（String） | ❌ 否 | ✅ 是 |
| `StringWriter` | 内存缓冲区 | ❌ 否 | ✅ 是 |
| `FileReader` | 文件 | ✅ 是 | ❌ 否 |
| `BufferedReader` | 包装其他 Reader | 取决于底层 | ✅（带缓冲） |


### 3.4 序列化与反序列化
#### 基本概念
**序列化（serialization）**是指将数据结构或对象状态转换成可取用格式（例如存成文件，存于缓冲，或经由网络中发送），以留待后续在相同或另一台计算机环境中，能恢复原先状态的过程。

相对地，从一系列字节提取数据结构的反向操作，即**反序列化（deserialization）**

仓颉语言有着DataModel数据中间层，这在诸如 C#、Java 等主流语言的标准序列化库（如 Newtonsoft.Json、Jackson）中是**没有显式暴露**的

数据中间层是好的设计，数据中间层作为桥梁可以将各种类型的数据从中间层转化而来，而非一种类型的数据直接转化为另一种类型的数据，有利于直接扩展序列化方法，统一处理数据

<font style="background-color:#f3bb2f;">简单来说，序列化就是把对象转化为DataModel,反序列化则是将DataModel转化为对象</font>

#### 使用
实现序列化首先要实现接口`interface Serializable`

```plain
public interface Serializable<T> {
    func serialize(): DataModel
    static func deserialize(dm: DataModel): T
}
```

> 说明
>
> 支持实现 [Serializable](https://developer.huawei.com/consumer/cn/doc/cangjie-guides-V5/serialization_package_interfaces-V5#interface-serializable) 的类型包括:
>
> + 基本数据类型：整数类型、浮点类型、布尔类型、字符类型、字符串类型。
> + [Collection](https://developer.huawei.com/consumer/cn/doc/cangjie-guides-V5/core_package_interfaces-V5#interface-collectiont) 类型：[Array](https://developer.huawei.com/consumer/cn/doc/cangjie-guides-V5/core_package_structs-V5#struct-arrayt)、[ArrayList](https://developer.huawei.com/consumer/cn/doc/cangjie-guides-V5/collection_package_class-V5#class-arraylistt)、[HashSet](https://developer.huawei.com/consumer/cn/doc/cangjie-guides-V5/collection_package_class-V5#class-hashsett-where-t--hashable--equatablet)、[HashMap](https://developer.huawei.com/consumer/cn/doc/cangjie-guides-V5/collection_package_class-V5#class-hashmapk-v-where-k--hashable--equatablek)、[Option](https://developer.huawei.com/consumer/cn/doc/cangjie-guides-V5/core_package_enums-V5#enum-optiont)。
> + 用户自定义的实现了 [Serializable](https://developer.huawei.com/consumer/cn/doc/cangjie-guides-V5/serialization_package_interfaces-V5#interface-serializable)<T> 的类型。
>

#### 示例
##### A.自定义类的序列化与Json转化
```typescript
import stdx.encoding.json.ToJson
import stdx.serialization.serialization.*
import stdx.encoding.json.*

// 自定义的类是需要实现Serializable接口的
// 实现Serializable接口的类需要实现serialize和deserialize方法
// 类中的属性需要是可序列化的，可反序列化的，可比较的，可哈希的，可打印的
class Danny <: Serializable<Danny> {
    var name: String = "Danny"
    var age: Int64 = 18
    public func serialize(): DataModel {
        return DataModelStruct().add(field<String>("name", name)) // 将name属性序列化为DataModel中的name字段
            .add(field<Int64>("age", age))
    }

    public static func deserialize(dm: DataModel): Danny {
        let dms = match (dm) {
            case data: DataModelStruct => data // 将DataModel结构转换为DataModelStruct结构
            case _ => throw DataModelException("Invalid data model")
        }

        let result = Danny()
        result.name = String.deserialize(dms.get("name")) // 将DataModel中的name字段反序列化为String类型
        result.age = Int64.deserialize(dms.get("age"))
        return result
    }
}

main() {
    // DataModel转换为Json字符串
    let danny = Danny()
    danny.name = "Danny"
    danny.age = 18
    let dm = danny.serialize()
    let DannyJson = dm.toJson()
    println("DannyJson: ${DannyJson}")

    // Json字符串转换为DataModel
    let dm2 = ##" {"name":"Danny","age":18}"##
    let dannyJson = JsonValue.fromStr(dm2) // 转换为JsonValue对象
    let dannyObj = (dannyJson as JsonObject).getOrThrow() // JsonObject对象
    let name = (dannyObj.get("name").getOrThrow() as JsonString).getOrThrow()
    let age = (dannyObj.get("age").getOrThrow() as JsonInt).getOrThrow()
    println("name: ${name} age: ${age}")
}

```

```bash
output：
DannyJson: {"name":"Danny","age":18}
name: "Danny" age: 18
```

##### B.HashSet与HashMap的序列化与反序列化
```typescript
import stdx.encoding.json.ToJson
import stdx.serialization.serialization.*
import stdx.encoding.json.*
main() {
    let DannyHashSet = HashSet<Danny>([Danny("Danny1"), Danny("Danny2")])
    let DannyHashSetData :DataModel = DannyHashSet.serialize()
    println("DannyHashSetData: ${DannyHashSetData.toJson().toString()}")
    println("=================================================================")
    let DannyHashMap = HashMap<String, Danny>([("Danny1", Danny("Danny1")), ("Danny2", Danny("Danny2"))])
    let DannyHashMapData :DataModel = DannyHashMap.serialize()
    println("DannyHashMapData: ${DannyHashMapData.toJson().toString()}")
}


// HashSet与HashMap的序列化与反序列化
class Danny <: Hashable & Equatable<Danny> & Serializable<Danny> {
    var name : String = "Danny"

    init(name: String) {
        this.name = name
    }

    public func hashCode(): Int64 {
        return name.hashCode()
    }

    public operator func ==(right: Danny): Bool {
        return name == right.name
    }

    public operator func !=(right: Danny): Bool {
        return name != right.name
    }

    public func serialize(): DataModel {
        return DataModelStruct().add(field<String>("name", name))
    }

    public static func deserialize(dm: DataModel): Danny {
        let dms = match (dm) {
            case data: DataModelStruct => data
             case _ => throw Exception("this data is not DataModelStruct")
        }
        let result = Danny("Danny")
        result.name = String.deserialize(dms.get("name"))
        return result
    }
}
```

## 4.仓颉语言库
### 4.1 随机数库
```typescript

// 引入随机数包
import std.random.Random
 
main(): Int64 {
    let bool = Random().nextBool()
    let int = Random().nextInt64()
    let float = Random().nextFloat64()
    let uint = Random().nextUInt64()
    
    // 生成[10, 60)的随机数，注意：左闭右开
    let num = Random().nextInt64(50) + 10
    
    //可以通过设置随机数种子，使得每次生成的随机数保持一致。比如：第一次运行生成10，第二次运行还是生成10。
    let int = Random(10)
    for(_ in 0..3) {
        println("${int.nextInt8()}")
    }
    println(bool)
    println(int)
    println(float)
    println(uint)
    return 0
}
```

### 4.2 数学库
```typescript

// 引入数学包
import std.math.*
// 引入格式化包
import std.format.Formatter
 
main(): Int64 {
    // 绝对值函数
    println(abs(-12))
    // 开平方 参数类型：浮点类型
    println(sqrt(4.0))
    // Π
    println(Float64.PI)
    // 正弦函数 参数类型：浮点类型 且 参数是弧度制
    println(sin(30.0 * Float64.PI / 180.0))
    // 向上取整 小数保留两位小数
    println(ceil(4.7).format(".2"))
    // 向下取整
    println(floor(6.1).format(".2"))
    // 四舍五入
    println(round(3.4).format(".2"))
    println(round(3.5).format(".2"))
    return 0
}
```

### 4.3 格式化库
```typescript

// 引入格式化包
import std.format.Formatter
import std.math.*
 
main(): Int64 {
    // 给正数前面加上“+”号
    println(123.format("+"))
    // 可以将十进制数转成二进制、八进制、十六进制
    println(2.format("B")) // 二进制
    println(3.format("O")) // 八进制
    println(10.format("X")) // 十六进制
    // 科学计数法
    println(Float64.E.format("E"))
    // 控制浮点数的精度
    println(Float64.PI.format(".4"))
    return 0
}
```

